# Documents index

Working notes for the FlexFit Studio refactor. Read in whatever order suits you; the table below says what each file is for.

| File | Purpose |
|---|---|
| [discovery-log.md](./discovery-log.md) | How current behavior was worked out — reading order, what surprised me, where source contradicted assumptions. |
| [behavior-spec.md](./behavior-spec.md) | Per-router contract: inputs, outputs, constants, error codes, exact message strings, edge cases. |
| [coverage-matrix.md](./coverage-matrix.md) | Every spec claim mapped to the test that proves it. |
| [architecture-decisions.md](./architecture-decisions.md) | What got consolidated, what deliberately didn't, and why. |
| [known-issues.md](./known-issues.md) | Pre-existing bugs found during discovery, with exact repro steps. |
| [decision-log.md](./decision-log.md) | Running log: decision chosen / alternative considered / why. |
| [ai-usage.md](./ai-usage.md) | Honest running log of AI tool use. |

Status as of 2026-08-11 (final, covering the Aug 9 and Aug 10/11 work sessions): the suite stands at **70 tests**, `pnpm test` green and stable across repeated runs, `npx tsc --noEmit` clean. Aug 9 closed with all four priority routers (`bookings`, `corporate-bookings`, `reschedules`, `payments`) carrying behavior specs and characterization tests (64 tests at that checkpoint), then the Aug 9 refactor landed — `hoursUntil`, the four time/credit constants, the reschedule validation ladder, and capacity counting extracted into `src/server/booking/`, each step verified with a full suite run and a clean typecheck before moving to the next. Aug 10/11 then fixed bug #9 (`admin.classUtilisation`) per the sequence in `known-issues.md` #9, ran a second discovery pass beyond the four priority routers that found six more findings (`known-issues.md` #10-#14, plus an expansion of #6) — documentation-only except #10 (most consequential; backed by `test/classes.test.ts`, bringing the total to 70), and closed with a regression pass over real HTTP against the running dev server — member and trainer verified end to end (full book/reschedule/cancel/corporate-book cycle; roster/check-in cycle), admin only partially (login, the bug #9 fix confirmed live, public schedule, one payments markPaid/refund cycle — `classes`/`admin-companies`/`notifications` admin actions not exercised live). `architecture-decisions.md` reflects what was actually built, including one plan revision made mid-extraction (its `capacity.ts` section) and a section on why transactions were never added. No router's public procedure signature, and no file under `src/app/` or `src/components/`, changed at all — only what was inside procedure bodies moved. No browser tool available, so UI rendering itself is unverified; judged low-risk since no frontend file changed this entire refactor. Full detail, including the discrepancy the regression pass turned up (expected, not a bug) and the pending `pnpm db:reset` to restore clean seed data, in `discovery-log.md`'s Aug 10 entry.
